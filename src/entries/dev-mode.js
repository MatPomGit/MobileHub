'use strict';

// Compatibility entrypoint kept for legacy script include and SW cache.
// Real implementation lives in src/dev/* modules.
window.initDevMode = window.initDevMode || function initDevModeCompat() {};
